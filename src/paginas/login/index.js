import React from 'react';

import Header from '../../componentes/header/'
import FormLogin from '../../componentes/forms-login/'

export default function Login () {
    return(
      <div>
        <Header />
        <FormLogin />
      </div>
    );
}